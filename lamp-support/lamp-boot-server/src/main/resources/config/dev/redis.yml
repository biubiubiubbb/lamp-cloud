lamp:
  cache:
    type: REDIS
  redis:
    ip: 127.0.0.1
    port: 6379
    password: ''
    database: 7

spring:
  cache:
    type: GENERIC
  data:
    redis:
      host: ${lamp.redis.ip}
      password: ${lamp.redis.password}
      port: ${lamp.redis.port}
      database: ${lamp.redis.database}
